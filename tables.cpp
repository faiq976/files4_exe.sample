#include <iostream>
using namespace std;
int main()
{

    int multiple;
    int table = 24;
    for (int num = 1; num <= 10; num - num + 1)

    {
        multiple = table * num;
        cout << table << "*" << num << "=" << multiple << endl;
    }
    int multiple;
    int table = 50;
    for (int num = 1; num <= 10; num = num + 1)

    {
        multiple = table * num;
        cout << table << "*" << num << "=" << multiple << endl;
    }

    int multiple;
    int table = 29;
    for (int num = 1; num <= 10; num = num + 1)
    {
        multiple = table * num;
        cout << table << "*" << num << "=" << multiple << endl;
    }
}
